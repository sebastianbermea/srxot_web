*** PAGINA ***

    //Deploy
    rm -rf build dist
    npm run build
    firebase deploy --only hosting   

*** PRODUCTOS ***

 - Titulo
 - Descripcion
 - Imagen principal
 - Precio: Puntual
 - Agregar producto:
    Agregar a functions a "IMAGENES_PRODUCTOS" el id
    * METADATOS *
    - size: 
        Texto de tamaño

    - best:
        [true, false]
        Icono Best Seller
    
    - popular:
        [true, false]
        Lista de productos popular

    - stock:
        [Numero]
        Numero disponible en dado caso de ser menor a 20 tendra el icono de casi agotado.
    
    - stickers:
        [true, false]
        Texto de esta botella tiene stickers

    - img_1,
      img_2,
      img_3:
        Link de imagenes extra de productos.
    
    - sizes:
        Con tener el metadato se pone la opcion para seleccionar talla [Para ropa]
    
    - discount:
        [price_id] con el descuento, ej "price_1SrMuGH5q3LGQ4gR3lLWswVX"
        Se crea un price id nuevo con el precio descontado.
        * Se necesita discount_limit *
    -discount_limit:
        [number] ej "1776232799"
        Timestamp en segundos para cuando se termina el descuento.



*** CUPONES ***

    Los cupones son para stripe,
    Los codigos de promocion son para el cliente, estos son los que funcionan y sirven para trackear diferentes aplicaciones y/o usos.
    Tambien sirve para cuando eliminas un cupon se eliminan todos los codigos derivados de el.
    - Opciones son auto explicatorias dentro del entorno como Primer pedido, fecha de vencimiento, pedido minimo, etc.
    - Regalo, poner oferta de 100% en producto para un solo uso por persona.


*** ENVIOS ***

    Se tendran dos ids de tarifas de envio la normal y la gratis dependiendo de el costo establecido,
    // Añadir a payments trackingNumber
    trackingNumber


*** ENTORNO DE PRUEBAS ***

    Actualizar clave secreta en stripe key en firebase
    Actualizar webhook secret en firebase
    Quitar productos de prueba
    Actualizar Ids de envio

*** FUNCTIONS ***
    
    createCheckoutSession
        collect_shipping_address = true,
        shipping_address_collection: { allowed_countries: ['MX'] },

        modificar: shipping_rates -> shipping_options 

    createCheckoutSession
    WebEvents
    -insertPaymentRecord
    -insertInvoiceRecord
        //Arriba de la funcion insertar payment  await customersSnap.docs[0].ref.collection('payments').doc(recordId)
    await customersSnap.docs[0].ref.update({
        lastPurchase: admin.firestore.FieldValue.serverTimestamp()
     });

    // Agregar correo para envios de mail automaticos
     if (checkoutSession.customer_details) {
      payment['receipt_email'] = payment.receipt_email || checkoutSession.customer_details.email || '';
    }
    // agregar tracking_number para Admin panel
    payment['tracking_number'] = null;

    // Deploy
    firebase deploy --only functions:sendMailSuccess  